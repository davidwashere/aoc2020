package util

// WIP

type Tokinator struct {
}

func ParseTokens() {
	// Given a string fo 'stuff', parse out the sections of ints and strings

	// Results will be
	// []int where ints[0] is first num in string, ints[1] is second and so on
	// []string where strings[0] is first string, strings[1] is second
	//

}
